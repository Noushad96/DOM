// ============ queryselector ========

// If you want to find all HTML elements that match a specified CSS selector ~
// (id, class names, types, attributes, values of attributes, etc),~
//   use the [querySelectorAll()] method.

// const query = document.querySelectorAll("#main-heading");
// console.log(query);

// document.getElementById("demo").innerHTML = "Hello World from ID!";

document.querySelector("#demo").innerHTML = "Hello World from ID!";
